
// Write your code here
let num1= 31;
let num2 = 2;
let multiply = (num1 * num2)

// generates a random integer greater than 0
// math.floor- rounds the number down
// Math.round()-rounds the number either up or down
let random = (Math.floor(Math.random() * 10) + 1);

//is an equation that calculates a remainder that is equal to 4
let num3 = 34;
let num4 = 6;
let mod = (num3 % num4)

//will return 20 as the highest number in the set
let x = 5;
let y = 6;
let z = 20;
// Math.max()-accepts the no of arguments and return highest lowest constituent
let max = (Math.max(x,y,z))